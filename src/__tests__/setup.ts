import '@testing-library/jest-dom';
import { vi } from 'vitest';

// Mock console.error to keep test output clean if needed, 
// but usually better to let it print unless specifically testing for it.
// For now, just the jest-dom import is standard.
